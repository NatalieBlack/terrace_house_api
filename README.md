# Terrace House JSON API

## Endpoints

The API base URL is [https://terracehouseapi.herokuapp.com](https://terracehouseapi.herokuapp.com). 

All endpoints output JSON.

* `/members`
* `/members/:id`
* `/series`
* `/series/:id`
